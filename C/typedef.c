// typedef - compiler, define - preprocessor

typedef int Length;

Length len, maxlen;
Length *lengths[];

// equals to

int len, maxlne.
int *lengths[];

typdef char *String;

String p lineptr[MAXLENGTH];
int strcmp(String, String);
p = (String) malloc(100);

typedef struct {
  long ino;
  char name[NAMX_MAX + 1];
} Dirent;

typedef struct {
  int fd;
  Dirent d
}
